version: "3.9"

services:
  scraper:
    build: .
    container_name: scraper-api
    restart: unless-stopped
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./data:/app/data
